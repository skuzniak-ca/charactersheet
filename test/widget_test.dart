// Placeholder test. Smoke testing is done by running the app on Linux desktop.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
